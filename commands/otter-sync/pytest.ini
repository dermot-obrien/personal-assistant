[pytest]
# Register custom markers
markers =
    live_otter: mark test as requiring live Otter API credentials (deselected by default)

# By default, exclude live_otter tests
# Run with: pytest -m live_otter to include them
addopts = -m "not live_otter"
