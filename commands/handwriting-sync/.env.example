# Handwriting Sync Local Test Configuration
# Copy this file to .env and fill in the values

# GitHub repository (owner/repo format)
GITHUB_REPO=your-username/your-logseq-repo

# GitHub Personal Access Token with repo read access
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# Google Cloud Project ID (required for OCR processing)
GCP_PROJECT=your-gcp-project-id

# Optional: Git branch to scan (default: main)
GITHUB_BRANCH=main

# Optional: Path to journal files in repo (default: journals)
LOGSEQ_JOURNAL_PATH=journals

# Optional: Timezone for timestamps (default: Pacific/Auckland)
LOCAL_TIMEZONE=Pacific/Auckland
